# Optional canonical URL used for metadata during local or non-GitHub builds.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Normally set automatically by the GitHub Pages workflow.
# Leave blank for local root hosting.
NEXT_PUBLIC_BASE_PATH=
